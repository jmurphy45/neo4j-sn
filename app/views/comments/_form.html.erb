<%= form_for Comment.new do |f| %>
  <div class="field">
    <%= f.text_area :comment, class: "form-control", rows:3 %>
  </div>
  <%= hidden_field_tag :post_id, post.uuid %>
  <div class="actions">
    <%= f.submit 'Comment', class: "btn btn-success" %>
  </div>
<% end %>
